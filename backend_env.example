# Porta server
PORT=3000

# Stringa connessione MongoDB (sostituisci con la tua nel .env reale)
DB_STRING=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>

# JWT secrets (scegli valori forti nel .env reale)
TOKEN_ACCESSO=access_token_secret
TOKEN_REFRESH=refresh_token_secret
